git add index.html
git commit -m "Add index.html"
git push origin main
Названия меняй только





# добавить несколько конкретных файлов
git add index.html simple.html

# или добавить все новые и изменённые файлы в папке рекурсивно
git add .

# зафиксировать изменения
git commit -m "Add pages"

# отправить на удалённый origin в ветку main (первый раз с установкой upstream)
git push -u origin main

# обычный push (если upstream уже настроен)
git push origin main